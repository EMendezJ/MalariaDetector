clear all
close all
clc

%parametros fisicos
g = 9.81;
hBo = 0.18; %altura del agua [m]
dBo = 0.05;
hOr = 0.03;
dOr = 0.003;
hAg = 0.145;


datos = readmatrix("mass_B.txt");
tDat = datos(:,1);
yDat = datos(:,2);
plot(tDat, yDat);

